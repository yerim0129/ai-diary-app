# ⚡ 백엔드 빠른 시작 가이드

## 🎯 5분 안에 백엔드 이해하기

### 1단계: 백엔드란?
**백엔드 = 식당의 주방**
- 손님(프론트엔드)이 주문하면
- 주방(백엔드)에서 요리를 만들어서
- 손님에게 전달

### 2단계: 현재 프로젝트 구조
```
프론트엔드 (브라우저)
    ↓ "일기 저장해줘" 요청
백엔드 (server/api 폴더)
    ↓ 처리
프론트엔드로 결과 반환
```

### 3단계: 이미 있는 백엔드
- ✅ `server/api/analyze.post.ts` - AI 감정 분석
- ✅ `server/api/hello.get.ts` - 테스트용 (방금 만듦!)

### 4단계: 테스트해보기
1. 개발 서버 실행: `npm run dev`
2. 브라우저에서 `http://localhost:3000` 접속
3. 홈페이지에서 "API 응답: ..." 메시지 확인

### 5단계: 다음에 추가할 것
1. 일기 저장 API
2. 일기 조회 API
3. 통계 API

---

## 📝 간단한 예제

### 예제 1: Hello API (이미 있음!)
**파일**: `server/api/hello.get.ts`
```typescript
export default defineEventHandler(() => {
  return { message: '안녕하세요!' }
})
```

**사용**: 프론트엔드에서
```javascript
const { data } = await useFetch('/api/hello')
console.log(data.value.message) // "안녕하세요!"
```

### 예제 2: 일기 저장 API (추가 필요)
**파일**: `server/api/diaries.post.ts`
```typescript
export default defineEventHandler(async (event) => {
  const diary = await readBody(event)
  // 여기서 저장 로직
  return { success: true, diary }
})
```

---

## 🚀 다음 단계

**BACKEND_SETUP_GUIDE.md** 파일을 열어서 자세한 가이드를 확인하세요!




